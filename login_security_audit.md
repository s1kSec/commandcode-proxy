# 登录与管理接口安全审计报告

## 1) 执行摘要

- 目标：`commandcode-proxy` 当前工作区的管理登录、会话、管理配置接口及其反向代理信任边界。
- 审计模式：标准范围审计（静态数据流追踪 + 本地黑盒回归）。
- 范围内：`/admin`、`/api/admin/login`、`/api/admin/logout`、`/api/admin/state`、`/api/admin/account-pool`、管理配置校验、Cookie、CSRF、来源 IP、代理头、在线配置持久化。
- 范围外：OpenAI/Anthropic 协议转换、SSE 内容语义、Command Code 官方服务本身及公网基础设施渗透测试。
- 原始问题实例：4 个低风险防御缺口，均已修复；未确认任何身份认证或权限绕过。
- 去重根因：4 个。
- 最高风险结论：当前代码未发现可在不知道管理密码的情况下读取管理状态或修改账号池的路径。公网部署仍必须使用 HTTPS、强密码哈希，并正确配置可信反向代理。

## 2) 攻击面清单

- HTTP/API：1 个公开登录页面、1 个登录接口、1 个只读状态接口、1 个配置写接口、1 个退出接口。
- 身份与权限：单管理员角色；scrypt 密码校验；随机服务端会话；HttpOnly、SameSite=Strict、Secure Cookie；写操作要求独立 CSRF Token。
- 文件写入：只有通过认证和 CSRF 校验的账号池设置可写入固定的 `data/account-pool.override.json`；请求不能控制路径。
- 外部边界：TLS 终止代理只有在直连地址精确命中 `trustedProxyIps` 时，其 `X-Forwarded-Proto` 和最终 `X-Forwarded-For` 才被信任。
- 敏感数据：管理状态只返回 `hasApiKey`，不返回账号 Key、账号池代理 Key、密码哈希或邮箱。

## 3) 发现与修复

### [F-001] 登录失败记录达到容量时整表清空

1. 位置：`proxy.mjs` 的 `recordFailedAdminLogin` / `adminLoginAttempts`。
2. 类型与根因：CWE-307（认证尝试限制不足）；旧逻辑在来源表超过 10,000 项时执行整表清空。
3. 证据链：大量不同直连来源 -> 登录失败记录 Map -> 容量判断 -> 所有限流状态被清空。
4. 利用前提：攻击者需要大量不同的真实直连来源；伪造 `X-Forwarded-For` 对不可信连接无效。
5. 影响：削弱暴力破解保护，但不会直接产生有效会话。
6. 严重性与证据：Low / Confirmed（代码证据）。
7. 最小修复：过期清理后只淘汰最旧记录，不再整表清空。
8. 回归：五次错误密码后，即使随后提供正确密码也返回 429 且不设置 Cookie。

### [F-002] 可信代理后的所有访客共享一个登录限流身份

1. 位置：`proxy.mjs` 的 `handleAdminLogin`、`getAdminLoginAddress`。
2. 类型与根因：CWE-400（资源/可用性控制）；旧逻辑始终使用容器直连地址，HTTPS 代理后的访客会共享代理 IP。
3. 证据链：公网访客 -> 可信 TLS 代理 -> 相同后端 peer IP -> 共享五次失败计数 -> 管理员登录被整体锁定。
4. 利用前提：管理端位于同一个受信反向代理后并开放公网。
5. 影响：管理登录最多被锁定 15 分钟；代理转发服务不受影响。
6. 严重性与证据：Low / Confirmed（控制流证据）。
7. 最小修复：仅当直连 peer 精确命中 `trustedProxyIps` 时，使用代理覆盖或追加的最后一个合法 `X-Forwarded-For` 地址进行限流。
8. 回归：受信代理后的不同最终客户端 IP 分开计数；改变头部前缀不能绕过已经触发的限流。

### [F-003] 管理 API 响应未统一禁止缓存

1. 位置：`proxy.mjs` 的 HTTP 路由入口和管理 JSON 响应。
2. 类型与根因：CWE-525（浏览器缓存中的敏感信息）；页面已禁止缓存，但管理 API 原先没有统一的 `Cache-Control: no-store`。
3. 证据链：已认证请求 -> 管理状态/CSRF 响应 -> 浏览器或中间缓存。
4. 利用前提：客户端或中间设施错误缓存认证响应，且攻击者能访问该缓存环境。
5. 影响：可能残留账号元数据和 CSRF Token；真实 Key 从未出现在响应中，单独的 CSRF Token 不能建立会话。
6. 严重性与证据：Low / Confirmed（响应头证据）。
7. 最小修复：所有 `/api/admin/*` 响应统一设置 `Cache-Control: no-store` 和 `X-Content-Type-Options: nosniff`。
8. 回归：未认证和已认证管理 API 均检查 `no-store`，且保持无 CORS。

### [F-004] 路由解析依赖不可信 Host，异常发生在统一错误处理外

1. 位置：`proxy.mjs` 的 `http.createServer` 路由入口。
2. 类型与根因：CWE-400（异常请求导致连接不可用）；旧代码使用 Host 作为 URL 基址，并在路由 `try` 之前解析。
3. 证据链：畸形 Host/请求目标 -> `new URL` 抛错 -> 异步处理器提前退出 -> 当前连接无规范错误响应。
4. 利用前提：攻击者可直接发送畸形 HTTP 请求。
5. 影响：单请求连接挂起或异常日志；未发现进程退出或认证绕过。
6. 严重性与证据：Low / Confirmed（异常路径代码证据）。
7. 最小修复：使用固定本地基址解析路径，并在失败时返回 400；路由决策不再依赖 Host。
8. 回归：语法、正常路由和全部管理认证测试通过。

## 4) 去重摘要

- R1：限流状态生命周期不稳健，对应 F-001。
- R2：代理后的客户端身份未安全细分，对应 F-002。
- R3：管理 API 缺少统一响应缓存策略，对应 F-003。
- R4：路由解析信任了不需要信任的 Host 输入，对应 F-004。

## 5) 修复优先级

- P0：无未修复项。
- P1：上线前启用 HTTPS；使用强随机管理密码生成有效 scrypt 哈希；仅填写真实后端代理 IP 到 `trustedProxyIps`；反向代理必须覆盖相应转发头。
- P2：在公网入口增加独立的连接/请求速率限制与告警；定期重启或主动注销以失效旧的内存会话。

## 6) 回归结果

- 管理状态无 Cookie：401。
- 随机 Cookie + 正确账号池代理 Key：401。
- 错误/缺失 CSRF 写配置：403。
- 未受信来源伪造 `X-Forwarded-Proto`：426。
- 伪造 `X-Forwarded-For` 绕过来源白名单：403。
- 尾斜杠和编码路径变体：404。
- 受信代理后的客户端限流隔离：通过。
- 登录失败限流：通过。
- 账号 Key、账号池代理 Key不出现在页面、状态或用量响应：通过。

## 7) 限制与假设

- 本报告验证的是当前工作区代码和本地 mock 上游；没有对生产 Nginx/Caddy、云防火墙或 Command Code 官方服务做入侵测试。
- `allowedIps: ["*"]` 是明确的业务选择，只代表任何来源可以到达登录入口，不代表任何来源通过了认证。
- 仓库示例配置中的 `adminAuth.enabled` 仍为 `false`，且没有默认密码。必须由部署者设置有效密码哈希后才能启用；这是故意的 fail-closed 行为。
